<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @language_families.each do |language_family| %>
  <tr>
    <td><%= language_family.name %></td>
    <td><%= language_family.description %></td>
    <td><%= link_to 'Show', language_family %></td>
    <td><%= link_to 'Edit', edit_language_family_path(language_family) %></td>
    <td><%= link_to 'Destroy', language_family, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Language family', new_language_family_path %>
