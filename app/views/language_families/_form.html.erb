<%= form_for(@language_family) do |f| %>
  <% if @language_family.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@language_family.errors.count, "error") %> prohibited this language_family from being saved:</h2>

      <ul>
      <% @language_family.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'inpupt-xlarge' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :description, :class => 'control-label' %>
    <div class="controls">  
      <%= f.text_area :description , :class => 'input-block-level' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.submit 'Submit', :class => 'btn btn-primary' %>
  </div>
<% end %>
